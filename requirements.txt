# --- starter kit core (bunq sandbox client) ---
requests>=2.28
cryptography>=41.0
python-dotenv>=1.0

# --- orchestrator server ---
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9
httpx>=0.27

# --- LLM ---
anthropic>=0.39

# --- ElevenLabs TTS + Scribe STT (called via httpx) ---
# (no SDK needed — see orchestrator/tts.py and orchestrator/stt.py)

# --- Google Calendar OAuth (Phase 6) ---
google-auth>=2.35
google-auth-oauthlib>=1.2
google-api-python-client>=2.150

# --- browser agent (Phase 5+) ---
# `playwright install chromium` is required after pip install.
playwright>=1.48

# --- testing ---
pytest>=8.0
